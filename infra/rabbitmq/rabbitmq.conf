# 큐별 메트릭을 /metrics 에 그대로 노출한다.
# 이걸 안 켜면 rabbitmq_queue_messages_ready 가 전체 합계로만 나와서
# "어느 큐가 막혔나" 를 알 수 없다. 큐가 수천 개면 카디널리티가 부담이지만
# 이 프로젝트는 큐가 다섯 개라 켜두는 게 낫다.
prometheus.return_per_object_metrics = true

# 관리 UI 는 컨테이너 안에서만 쓰므로 기본값 유지
management.tcp.port = 15672
